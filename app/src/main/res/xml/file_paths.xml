<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Grants access to the app's cache directory where PDF invoices live -->
    <cache-path name="cached_invoices" path="invoices/" />
    <!-- Grants access to private files where photos live -->
    <files-path name="captured_photos" path="photos/" />
</paths>
